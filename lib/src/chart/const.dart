const eventAfterInit = 'afterInit';

const eventBeforeRender = 'beforeRender';
const eventAfterRender = 'afterRender';

const eventBeforeDataChange = 'beforeDataChange';
const eventAfterDataChange = 'afterDataChange';

const eventAfterSizeChange = '_afterSizeChange';

const eventAfterGeomInit = '_afterGeomInit';
